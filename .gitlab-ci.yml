image: node

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script: npm start
  artifacts:
    paths:
      - dist/demo-0.0.1-SNAPSHOT.jar

test: 
  stage: test
  script : npm test

production:
  stage: deploy
  script:
  - curl --location "https://cli.run.pivotal.io/stable?release=linux64-binary&source=github" | tar zx
  - ./cf login -u debanjana.maitra@gmail.com -p Ttsp1.1PCF -a api.run.pivotal.io -s master
  - ./cf push
  only:
  - master
